name: First Workflow

on: 
  push:
    branches:
      - main

jobs: 
  example-jobs:
    runs-on: ubuntu-latest

    steps:
      - name: Print a welcome msg
        run: echo "Welcome to our first GitHub Actions Workflows!"
      
